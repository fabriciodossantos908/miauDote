import {makeStyles} from '@material-ui/core/styles';

const useStyle = makeStyles((theme) => ({
    firstName:{
        fontFamily: "italic",
        fontSize: 20,
        color: "#d32f2f"
    },

twoName:{
    fontFamily: "italic",
    fontSize: 20,
    color: "#42a5f5",
}

}));

export {useStyle}